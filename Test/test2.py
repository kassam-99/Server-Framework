"""Placeholder module in the Test folder.

The real automated test-suite lives in the top-level ``tests/`` directory and is
run with ``pytest``. This file is intentionally minimal so it imports cleanly.
"""

if __name__ == "__main__":
    print("Run the project test-suite with: pytest")
